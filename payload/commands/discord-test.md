---
description: Send a test Discord DM to verify the notifier is wired up.
---

Run this exact bash command and nothing else, then report the stdout/stderr in one short line:

```bash
python3 "$HOME/.claude/notifier/send_discord.py" "test from /discord-test"
```
